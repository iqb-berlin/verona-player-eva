export enum PropertyKey {
  TEXT,
  TEXT2,
  MAX_VALUE,
  MIN_VALUE,
  LENGTH,
  LINE_COUNT,
  HELP_TEXT
}

export enum FieldType {
  TEXT,
  HEADER,
  TITLE,
  INPUT_TEXT,
  INPUT_NUMBER,
  HTML,
  HR,
  CHECKBOX,
  MULTIPLE_CHOICE,
  REPEAT_CONTROL,
  SCRIPT_ERROR
}
